
# edit_template tests

# build_eml tests

# check_eml tests
# Test that it correctly identifies if all fields are present and prints a
# message
# Test that it correctly identifies if one or more field are missing and prints
# a message
# Test that it correctly identifies if fields are missing due to: 1) NULL 2)
# empty list 3) empty string

# validate_output_path tests - move this function to helper functions file

# add tests for the validate output path function
